PUPPETEER_SKIP_DOWNLOAD=true
PUPPETEER_SKIP_CHROMIUM_DOWNLOAD=true
# better-sqlite3 is listed under optionalDependencies so pnpm/npm will not
# abort the install if native compilation fails (e.g. on Android/Termux).
# sql.js (pure WebAssembly) is used as the automatic fallback in that case.
